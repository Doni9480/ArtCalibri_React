import React, { Component } from 'react';
import TitleBlock from '../../components/block_titlle/title_block';

export default class ShoppingCart extends Component {
   render() {
      return (
         <TitleBlock text={'ShoppingCart'} />
      )
   }
}
